{
  "name": "uss",
  "version": "1.0.0",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "URL Shortener Service - Cloudflare Worker",
  "devDependencies": {
    "wrangler": "^4.64.0"
  }
}
